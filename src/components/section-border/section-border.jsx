import styles from "./section-border.module.css";

const SectionBorder = () => {
  return (
    <div className={styles.wrapper}>
      <p className={styles.border}></p>
    </div>
  );
};

export default SectionBorder;
