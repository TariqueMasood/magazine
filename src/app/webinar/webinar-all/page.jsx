import { Container } from "react-bootstrap";
import styles from "./webinar-all.module.css";

const WebinarAll = () => {
  return (
    <div className={styles.wrapper}>
      <Container>Webinar all</Container>
    </div>
  );
};

export default WebinarAll;
